from setuptools import find_packages, setup

setup(
    name='kode',
    packages=find_packages(),
    version='0.1.0',
    description='We present a general framework for the transport of probability measures towards minimum divergence generative modeling and sampling using ordinary differential equations (ODEs) and Reproducing Kernel Hilbert Spaces (RKHSs).',
    author='Biraj Pandey',
    license='MIT',
)
